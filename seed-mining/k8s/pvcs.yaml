apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seed-mining-output
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1500Gi
