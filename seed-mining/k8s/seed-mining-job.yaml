apiVersion: batch/v1
kind: Job
metadata:
  name: seed-mining-sd21
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: seed-mining
        task: image-generation
        model: sd21
        gpu: a100
    spec:
      restartPolicy: Never

      containers:
      - name: generator
        image: seed-mining:latest  # Replace with your registry path
        imagePullPolicy: Always
        command: ["/bin/bash"]
        args:
        - "-c"
        - |
          echo "========================================="
          echo "Seed Mining — Image Generation (1 GPU)"
          echo "Model: ${MODEL_ID}"
          echo "Seeds: 0..${SEED_RANGE}-1"
          echo "========================================="
          echo ""

          # GPU verification
          echo "GPU Status:"
          nvidia-smi
          echo ""

          # Start generation
          echo "Starting image generation..."
          echo ""
          uv run python scripts/generate_seed_mining_images.py \
            --model_id "${MODEL_ID}" \
            --out_dir "${OUTPUT_DIR}" \
            --seed_range "${SEED_RANGE}" \
            --height "${HEIGHT}" \
            --width "${WIDTH}" \
            --num_inference_steps "${NUM_INFERENCE_STEPS}" \
            --guidance_scale "${GUIDANCE_SCALE}" \
            --batch_size "${BATCH_SIZE}" \
            --generator_device "${GENERATOR_DEVICE}" \
            --scheduler "${SCHEDULER}" \
            --image_format "${IMAGE_FORMAT}"

        resources:
          requests:
            nvidia.com/gpu: "1"
            memory: "32Gi"
            cpu: "4"
          limits:
            nvidia.com/gpu: "1"
            memory: "48Gi"
            cpu: "8"

        volumeMounts:
        - name: cache-volume
          mountPath: /cache
        - name: outputs-volume
          mountPath: /output
        - name: dev-shm
          mountPath: /dev/shm

        env:
        # HuggingFace token (required for gated models)
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
              optional: true

        # Cache directories (using ephemeral emptyDir)
        - name: HF_HOME
          value: "/cache/hf"
        - name: TRANSFORMERS_CACHE
          value: "/cache/hf/transformers"
        - name: TORCH_HOME
          value: "/cache/torch"

        # Model configuration
        - name: MODEL_ID
          value: "stabilityai/stable-diffusion-2-1"
        - name: OUTPUT_DIR
          value: "/output"
        - name: SEED_RANGE
          value: "100"
        - name: HEIGHT
          value: "512"
        - name: WIDTH
          value: "512"
        - name: NUM_INFERENCE_STEPS
          value: "30"
        - name: GUIDANCE_SCALE
          value: "7.5"
        - name: BATCH_SIZE
          value: "16"
        - name: GENERATOR_DEVICE
          value: "cpu"
        - name: SCHEDULER
          value: "ddim"
        - name: IMAGE_FORMAT
          value: "jpg"

        # Python settings
        - name: PYTHONUNBUFFERED
          value: "1"
        - name: PYTORCH_CUDA_ALLOC_CONF
          value: "expandable_segments:True"

      tolerations:
      - effect: NoSchedule
        key: nautilus.io/hardware
        operator: Equal
        value: large-gpu
      - effect: PreferNoSchedule
        key: nvidia.com/gpu
        operator: Exists
      - key: nautilus.io/reservation
        operator: Exists
        effect: NoSchedule

      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-A100-SXM4-80GB

      volumes:
      - name: cache-volume
        emptyDir:
          sizeLimit: 100Gi
      - name: outputs-volume
        persistentVolumeClaim:
          claimName: seed-mining-output
      - name: dev-shm
        emptyDir:
          medium: Memory
          sizeLimit: 16Gi
